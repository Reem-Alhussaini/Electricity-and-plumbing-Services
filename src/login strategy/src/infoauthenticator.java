public class infoauthenticator implements Authenticator {
    @Override
    public boolean authenticate(String username, String password) {
        // Authenticating using database
        return true; // Placeholder for actual authentication logic
    }
}
